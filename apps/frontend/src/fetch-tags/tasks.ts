/**
 * ---
 *
 * ## TasksTags
 *
 * Tags para o Next.js Data Fetching. Ele irá cachear as requisições, sendo assim, para atualizar o cache, basta revalidar as respectivas tags.
 *
 * Para saber mais sobre o cache das requisições fetch no Next.js, veja [a documentação do Next.js sobre fetch](https://nextjs.org/docs/app/api-reference/functions/fetch#optionsnexttags)
 *
 * ---
 */
export enum TasksTags {
  ALL = 'ALL',
}
